'use client';

// This component has been replaced by AnonymousDisplay.tsx
// This is a simplified version that just returns the AnonymousDisplay component

import AnonymousDisplay from './AnonymousDisplay';

export default function AuthButton() {
  return <AnonymousDisplay />;
}